# Hybrid local DX — stateful deps only (TARGET §1.2).
# Rails + Vite run natively on the host via bin/dev (wired in later P0 sessions).
#
#   docker compose -f docker-compose.dev.yml up -d
#   docker compose -f docker-compose.dev.yml down -v   # reset state

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: rajya-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rajya_development
    ports:
      # Host port 55432 (not 5432) — avoids clashing with native/Homebrew
      # Postgres installs, which on this machine occupy *both* 5432 and 5433.
      - "55432:5432"
    volumes:
      - rajya_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rajya_development"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: rajya-redis
    restart: unless-stopped
    ports:
      # Host port 6380 (not 6379) — avoids clashing with a native/Homebrew
      # Redis already listening on 6379 on some dev machines.
      - "6380:6379"
    volumes:
      - rajya_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  mailpit:
    image: axllent/mailpit:latest
    container_name: rajya-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # UI
    healthcheck:
      test: ["CMD", "/mailpit", "readyz"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  rajya_pg_data:
  rajya_redis_data:
