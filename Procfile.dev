# web / worker / vite — filled when backend (0.2) and frontend (0.4) scaffold.
# Until then `bin/dev` is not expected to boot application processes.
web: echo "session 0.2: backend web process"
worker: echo "session 0.2: solid queue worker"
vite: echo "session 0.4: vite dev server"
