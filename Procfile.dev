# web / worker / vite — run via `bin/dev` (foreman). Stateful deps (Postgres,
# Redis, Mailpit) run in Docker: `docker compose -f docker-compose.dev.yml up -d`.
web: cd backend && bin/rails server -p 3000
worker: cd backend && bin/jobs
vite: cd frontend && npm run dev
